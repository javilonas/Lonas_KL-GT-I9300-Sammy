#
# ADDITIONAL_DEFAULT_PROPERTIES
#
ro.secure=0
ro.allow.mock.location=0
ro.debuggable=0
persist.sys.usb.config=mtp,adb

# Enable USB debugging
persist.service.adb.enable=1

# Disable USB debugging icon on statusbar
persist.adb.notify=0